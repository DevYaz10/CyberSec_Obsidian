>[!quote] Definition
> Subdomain enumeration is the process of finding valid subdomains for a domain, 
> 
> but why do we do this? We do this to expand our attack surface to try and discover more potential points of vulnerability.


## OSINT

### SSL/TLS Certificates

in this lesson we will explore 3 different methods to enumerate subdomains:
- Brute Force 
- OSINT
- Virtual Host.

>[!info]-
> When an SSL/TLS (Secure Sockets Layer/Transport Layer Security) certificate is created for a domain by a CA (Certificate Authority), CA's take part in what's called "Certificate Transparency (CT) logs". These are publicly accessible logs of every SSL/TLS certificate created for a domain name. The purpose of Certificate Transparency logs is to stop malicious and accidentally made certificates from being used.

[this is a searchable database for ssl/tls certificates](https://crt.sh/)


### Search Engines

>[!info]-
> Search engines contain trillions of links to more than a billion websites, which can be an excellent resource for finding new subdomains.

you can use "site: filter" for example